import namespace from './namespace'

export default {
  'FAILED': `${namespace}/FAILED`,
  'FULLFILLED': `${namespace}/FULLFILLED`,
  'INVALIDATED': `${namespace}/INVALIDATED`,
  'PENDING': `${namespace}/PENDING`,
}

